{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "dist/src/index.js",
  "scripts": {
    "html": "copyfiles src/**/*.html src/**/*.js src/**/*.css src/**/*.ejs src/**/*.jpg dist/",
    "clean": "rimraf dist/*",
    "copy-assets": "ts-node tools/copyAssets",
    "lint": "tslint -c tslint.json -p tsconfig.json --fix",
    "tsc": "tsc",
    "build": "npm-run-all clean lint tsc html",
    "dev:start": "npm-run-all build start",
    "dev": "nodemon --watch src -e ts,ejs --exec npm run dev:start",
    "start": "node .",
    "test": "nodemon -w dist jasmine -config=jasmine.json",
    "test-unit": "npm run lint-test && karma start",
    "test-unit-debug": "npm run lint-test && karma start --single-run=false --debug --auto-watch",
    "lint-test": "tslint --format stylish --project tsconfig.json",
    "serve": "node express-serve.js",
    "startTestGeneration": "node express-serve.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@webcomponents/custom-elements": "^1.2.4",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.16.1",
    "@types/jasmine": "^3.3.13",
    "@types/node": "^12.0.4",
    "@webcomponents/webcomponentsjs": "^2.2.10",
    "copyfiles": "^2.1.0",
    "jasmine-core": "^3.4.0",
    "jsdom": "^15.1.1",
    "karma": "^4.1.0",
    "karma-chrome-launcher": "^2.2.0",
    "karma-coverage": "^1.1.2",
    "karma-jasmine": "^2.0.1",
    "karma-jsdom-launcher": "^7.1.1",
    "karma-sourcemap-loader": "^0.3.7",
    "karma-spec-reporter": "^0.0.32",
    "karma-typescript": "^4.1.0",
    "nodemon": "^1.19.1",
    "npm-run-all": "^4.1.5",
    "rimraf": "^2.6.3",
    "tslint": "^5.17.0",
    "tslint-config-standard-plus": "^2.2.0",
    "typescript": "^3.5.1"
  }
}
